/*(function () {
    'use strict';

    angular
        .module('app')

*/        /* @ngInject */
/*        .constant('SUBIDAPROTOTEXTO_FORM_ADMIN', function (modal) {
/*            var resolve = "$resolve.";
            if (modal) {
                resolve = "ctrl.resolve."
            }
            return {
                component: '<subida-prototexto-form-admin' +
                'item="' + resolve + 'item" ' +
                'previous-params="' + resolve + 'previousParams"></subida-prototexto-form-admin>',
                resolve: {
*/                   /* @ngInject */
/*                    item: function (SubidaPrototexto) {
                        return new SubidaPrototexto();
                    }
                }
            }
        });
})();
*/
